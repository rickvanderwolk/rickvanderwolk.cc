# Philips Hue ambient visual

## Setup

```bash
pip install -r requirements.txt
```

## Run

```bash
# With Hue Bridge
python server.py

# Demo mode (no bridge needed)
python server.py --mock
```

Open http://localhost:5555 in browser. Double-click for fullscreen.
